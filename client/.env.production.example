# Copy to `.env.production` before `npm run build` (values are baked into the JS bundle).
# Use your live API origin — no trailing slash.
VITE_API_URL=https://api.yourdomain.com
VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
